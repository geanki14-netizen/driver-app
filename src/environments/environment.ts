// Ambiente de DESARROLLO LOCAL
// Se usa por defecto con: ionic serve / ng build (sin --configuration)

export const environment = {
  production: false,
  envName: 'development',
  apiUrl: 'https://api-dev.tuapp.com/v1',
  appVersion: '1.0.0',
  enableLogs: true,
  notificationSenderId: 'TU_SENDER_ID_DEV',
};